spring:
  cloud:
    nacos:
      config:
        server-addr: 192.168.41.133:8848
        namespace: tutor-it
        group: TUTOR-BACK
  application:
    name: consumer-app
  profiles:
    active: dev
consumer:
  config: zhengzhou